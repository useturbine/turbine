{
  "name": "@useturbine/turbine",
  "version": "0.0.4",
  "description": "TypeScript SDK for Turbine",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "ts-node test.ts",
    "build": "tsc -p .",
    "publish": "yarn build && npm version patch --no-git-tag-version && npm publish --access public"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.197",
    "@useturbine/turbine": "^0.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
